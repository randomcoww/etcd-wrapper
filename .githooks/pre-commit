podman run -it --rm \
  -v $(pwd):$(pwd) \
  -w $(pwd) \
  docker.io/golang:alpine \
  gofmt -s -w .

podman run -it --rm \
  -v $(pwd):$(pwd) \
  -w $(pwd) \
  --entrypoint='' \
  ghcr.io/opentofu/opentofu:latest \
  tofu fmt -recursive